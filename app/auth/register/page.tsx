import { RegisterForm } from '@/components/auth/register-form'

export const metadata = {
  title: 'Register - LexAxiom',
  description: 'Create a new LexAxiom account',
}

export default function RegisterPage() {
  return <RegisterForm />
}
