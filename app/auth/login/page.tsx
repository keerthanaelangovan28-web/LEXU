import { LoginForm } from '@/components/auth/login-form'

export const metadata = {
  title: 'Sign In - LexAxiom',
  description: 'Sign in to your LexAxiom account',
}

export default function LoginPage() {
  return <LoginForm />
}
